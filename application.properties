# PayPal API credentials
paypal.clientId=YOUR_PAYPAL_CLIENT_ID
paypal.clientSecret=YOUR_PAYPAL_CLIENT_SECRET

# PayPal SDK Configuration
spring.profiles.active=dev # Specify your active profile (e.g., dev or prod)
paypal.mode=sandbox # Use "live" for production

# PayPal SDK Configuration
spring:
profiles:
active: dev # Specify your active profile (e.g., dev or prod)
paypal:
mode: sandbox # Use "live" for production

# Feign Client Configuration
book-service.url=http://book-service-url